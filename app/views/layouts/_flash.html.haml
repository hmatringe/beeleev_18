- if alert || notice
  .alert.fade.in{class: alert.present? ? "alert-warning" : "alert-success"}
    %button.close{type: "button", data: {dismiss: "alert"}, "aria-hidden" => "true"} &times;
    = flash[:html_safe] ? (alert || notice).html_safe : (alert || notice)
